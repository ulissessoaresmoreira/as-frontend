import { Button } from "@mui/material";

type Props = {
  refreshAction: () => void;
};

export const EventAdd = ({refreshAction}: Props) => {
  return (
    <>
    <div>Criar um evento. </div>
    </>
  );
};